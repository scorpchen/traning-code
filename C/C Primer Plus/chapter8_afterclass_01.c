/**********************************************
 * 
 * 8.11.1
 * 设计一个程序，统计在督导文件结尾之前读取的字符数。
 * 
 ***********************************************/

#include <stdio.h>

int main(){
    int count = 0;

    while(getchar() != EOF)
        count++;
    
    printf("%d", count);


    return 0;
}